# Hard-enforce the package.json `engines.node` floor (>=24). Below it, Node's
# native TypeScript type-stripping is off, so `pulumi up` + the infra tests fail to
# ESM-load the .ts graph with ERR_UNKNOWN_FILE_EXTENSION. Fail at install, not later.
engine-strict=true
