{
  "name": "notation-hero",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.11",
  "workspaces": ["apps/*", "adapters/*", "core/*", "packages/*", "infra"],
  "scripts": {
    "lint": "bun run --filter='*' lint",
    "typecheck": "bun run --filter='*' typecheck",
    "test": "bun run --filter='*' test",
    "build": "bun run --filter='*' build"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
