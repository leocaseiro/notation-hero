// Shared story-id list — kept in lockstep with Menubar.stories.tsx, Menubar.vr.ts, and
// Menubar.a11y.ts so the three suites never drift.
export const MENUBAR_STORY_IDS = ['open', 'with-selections'] as const;
