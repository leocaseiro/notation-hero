// Shared story-id list — kept in lockstep with Separator.stories.tsx, Separator.vr.ts, and
// Separator.a11y.ts so the three suites never drift.
export const SEPARATOR_STORY_IDS = ['horizontal', 'vertical'] as const;
