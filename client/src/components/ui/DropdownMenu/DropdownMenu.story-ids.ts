// Shared story-id list — kept in lockstep with DropdownMenu.stories.tsx, DropdownMenu.vr.ts,
// and DropdownMenu.a11y.ts so the three suites never drift.
export const DROPDOWN_MENU_STORY_IDS = ['open', 'with-checkbox-radio', 'with-submenu'] as const;
