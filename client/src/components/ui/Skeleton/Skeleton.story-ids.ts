// Shared story-id list — kept in lockstep with Skeleton.stories.tsx, Skeleton.vr.ts, and
// Skeleton.a11y.ts so the three suites never drift.
export const SKELETON_STORY_IDS = ['default', 'table', 'form'] as const;
