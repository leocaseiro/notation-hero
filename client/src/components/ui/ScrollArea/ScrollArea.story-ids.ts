// Shared story-id list — kept in lockstep with ScrollArea.stories.tsx, ScrollArea.vr.ts, and
// ScrollArea.a11y.ts so the three suites never drift.
export const SCROLL_AREA_STORY_IDS = ['horizontal-strip'] as const;
