// Shared LevelFilter story IDs (kebab) so VR + a11y stay in lockstep with LevelFilter.stories.tsx.
export const LEVEL_FILTER_STORY_IDS = [
  'unset',
  'max-only',
  'min-only',
  'range',
  'only',
  'debut',
] as const;
