// Shared story-id list — kept in lockstep with HoverCard.stories.tsx, HoverCard.vr.ts, and
// HoverCard.a11y.ts so the three suites never drift.
export const HOVER_CARD_STORY_IDS = ['open'] as const;
