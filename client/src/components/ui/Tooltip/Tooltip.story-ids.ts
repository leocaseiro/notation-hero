// Shared story-id list — kept in lockstep with Tooltip.stories.tsx, Tooltip.vr.ts, and
// Tooltip.a11y.ts so the three suites never drift.
export const TOOLTIP_STORY_IDS = ['default', 'long-text'] as const;
