# SpecStory auto-save
.specstory

# Dependencies
node_modules/
# pnpm store left in the repo root by the Dockerized VR-baseline run (see AGENTS.md)
.pnpm-store/

# Build output
dist/
dist-lambda/
*.tsbuildinfo

# pnpm-lock.yaml is COMMITTED (text lockfile) — see docs/cicd-pipeline.md

# Pulumi
.pulumi/
*.yaml.bak

# Env / secrets — never commit AWS creds
.env
.env.*
*.env
jira.env
!.env.example

# Private strategic context (the reference tutor north-star / positioning-wedge / handoff content).
# Public repo cannot contain competitor refs (Roland / [scrubbed-ref] / etc.).
# Mirror lives in a Linear Document under the Notation Hero project.
docs/.private/

# Editor / OS
.DS_Store
*.log

# Compound Engineering local config
.compound-engineering/*.local.yaml
