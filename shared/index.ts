// @notation-hero/shared — placeholder package.
//
// The oRPC contract + Zod schemas (ADR ARCH-CONTRACT-1, §3) will live here,
// imported by both client/ and server/. Nothing is wired yet — this is the
// workspace seam established in Phase 0.
export {};
