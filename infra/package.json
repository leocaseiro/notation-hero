{
  "name": "@notation-hero/infra",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "echo \"infra: lint configured in Wave 2 (Lane A)\"",
    "typecheck": "tsc --noEmit",
    "test": "echo \"infra: tests configured in Wave 2 (Lane A)\""
  },
  "devDependencies": {
    "@types/node": "^22.10.0"
  }
}
