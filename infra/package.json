{
  "name": "@notation-hero/infra",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "echo \"@notation-hero/infra: dedicated ESLint lands with real IaC source\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "pulumi:preview": "pulumi preview",
    "pulumi:up": "pulumi up",
    "pulumi:destroy": "pulumi destroy"
  },
  "dependencies": {
    "@pulumi/aws": "^7.34.0",
    "@pulumi/pulumi": "^3.247.0"
  },
  "devDependencies": {
    "vitest": "^4.1.9"
  }
}
