{
  "name": "@notation-hero/infra",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "echo \"@notation-hero/infra: real ESLint target lands with the flat-config lane (KAN-158)\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "node --test \"*.test.ts\"",
    "preview": "nx build @notation-hero/handler-hello && pulumi preview",
    "deploy": "nx build @notation-hero/handler-hello && pulumi up",
    "destroy": "pulumi destroy"
  },
  "dependencies": {
    "@pulumi/aws": "^7.32.0",
    "@pulumi/pulumi": "^3.246.0"
  }
}
