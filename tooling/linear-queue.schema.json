{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://notation-hero/linear-queue.schema.json",
  "title": "Linear Fallback Queue",
  "description": "Schema for tooling/linear-queue.json — used when the Linear MCP server is unavailable. See tooling/README.md.",
  "type": "object",
  "required": ["version", "queue"],
  "additionalProperties": true,
  "properties": {
    "$schema": { "type": "string" },
    "description": { "type": "string" },
    "version": {
      "type": "integer",
      "const": 1
    },
    "queue": {
      "type": "array",
      "items": { "$ref": "#/$defs/queueItem" }
    }
  },
  "$defs": {
    "queueItem": {
      "type": "object",
      "required": ["id", "action", "payload", "queuedAt", "queuedBy", "reason"],
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "string",
          "description": "Locally unique queue item id: `q-` prefix + 26-char ULID (Crockford base32, e.g. q-01HZK3MECHZX3TBDSZ7XR8QY3D). The first 10 chars encode a millisecond timestamp (sortable); the last 16 chars are crypto-random. F-2 hardening — replaced the prior sequential `q-YYYY-MM-DD-NNN` scheme that had a concurrent-enqueue race.",
          "pattern": "^q-[0-9A-HJKMNP-TV-Z]{26}$"
        },
        "action": {
          "type": "string",
          "enum": ["create_issue", "update_issue", "add_comment", "set_status", "add_label"],
          "description": "Linear MCP action this item replays when drained."
        },
        "payload": {
          "type": "object",
          "description": "Arguments for the Linear MCP call. Shape depends on action; minimally include team key + the resource fields."
        },
        "queuedAt": {
          "type": "string",
          "format": "date-time",
          "description": "ISO-8601 UTC timestamp when the item was queued."
        },
        "queuedBy": {
          "type": "string",
          "description": "Agent session id OR human handle that queued the item, for audit."
        },
        "reason": {
          "type": "string",
          "enum": ["mcp-unreachable", "token-expired", "rate-limited", "unknown-error"],
          "description": "Why the live MCP call failed (informational)."
        },
        "attempts": {
          "type": "integer",
          "minimum": 0,
          "default": 0,
          "description": "Number of replay attempts."
        },
        "lastError": {
          "type": ["string", "null"],
          "default": null,
          "description": "Last replay error string (null if never attempted or last succeeded)."
        },
        "linearId": {
          "type": ["string", "null"],
          "default": null,
          "description": "Linear-assigned issue id (e.g. LEO-123) after a successful create_issue replay. Once set, subsequent replays use update_issue instead of create_issue, making the queued operation idempotent against partial-success / crash-before-remove races (F-1 hardening)."
        }
      }
    }
  }
}
