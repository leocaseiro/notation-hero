{
  "name": "@notation-hero/handler-hello",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "echo \"@notation-hero/handler-hello: real ESLint target lands with the flat-config lane (NH-42)\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test \"src/**/*.test.ts\"",
    "build": "esbuild src/index.ts --bundle --platform=node --target=node24 --format=cjs --outfile=dist/index.js && node -e \"require('fs').writeFileSync('dist/package.json',JSON.stringify({type:'commonjs'}))\""
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
